import React, { memo, useCallback } from 'react';


import './index.less';

const Operate = props => {

    let { reload, spinReload } = props

    return (
        <div>
           
        </div>
    )
}

export default memo(Operate);